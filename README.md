# go-hsr-shared

Shared runtime utilities for HSR modules and final assembly projects.

Packages:

- `pkg/database`: open SQLite/PostgreSQL Bun databases.
- `pkg/schema`: apply/reset Bun model schemas.
- `pkg/requestctx`: request metadata context and trusted proxy client IP parsing.
- `pkg/httpserver`: small HTTP server middleware helpers.
